[build]
  command = "npm install && npm run build"
  publish = "dist/public"

[build.environment]
  BASE_PATH = "/"
  PORT      = "3000"
  NODE_ENV  = "production"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
